import { env } from "cloudflare:workers";import{NextRequest,NextResponse}from"next/server";import{hasSiteSessionRequest}from"@/lib/site-auth";
type Runtime={KITCHEN_API_TOKEN?:string;MEMBERS_API_BASE_URL?:string};
async function forward(request:NextRequest){if(!await hasSiteSessionRequest(request))return NextResponse.json({error:"LOGIN_REQUIRED"},{status:401});const runtime=env as unknown as Runtime;if(!runtime.KITCHEN_API_TOKEN)return NextResponse.json({error:"KITCHEN_API_NOT_CONFIGURED"},{status:503});const base=runtime.MEMBERS_API_BASE_URL??"https://compassion-world-members-card.combetter27.chatgpt.site";try{const response=await fetch(`${base}/api/v1/kitchen/test-orders`,{method:request.method,headers:{Authorization:`Bearer ${runtime.KITCHEN_API_TOKEN}`,...(request.method==="POST"?{"Content-Type":"application/json"}:{})},body:request.method==="POST"?await request.text():undefined});return new NextResponse(await response.text(),{status:response.status,headers:{"Content-Type":"application/json","Cache-Control":"no-store"}})}catch{return NextResponse.json({error:"KITCHEN_API_UNAVAILABLE"},{status:502})}}
export function POST(request:NextRequest){return forward(request)}export function DELETE(request:NextRequest){return forward(request)}
